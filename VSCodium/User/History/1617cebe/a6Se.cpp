#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int32_t main() {
	ios::sync_with_stdio(false); cin.tie(0);
	int n; cin >> n;
	int w = 0;

	for(;n--;) {
		int a, k; cin >> a >> k;
		if(a < k) w ^= 0;
		
	}
}