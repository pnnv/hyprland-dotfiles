#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

void snu() {
  int n; cin >> n;
  vector <int> l(n), r(n), a(n), b(n);
  for (int i = 0; i < n; i++)
    cin >> l[i] >> r[i] >> a[i] >> b[i];

  
}

int32_t main() {
  ios::sync_with_stdio(false); cin.tie(0);
  int t; cin >> t;
  while(t--) snu();
}