#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int32_t main() {
  ios::sync_with_stdio(false); cin.tie(0);
  
  int n, c; cin >> n >> c;
  vector <int> v(n);
  for(auto &i: v) cin >> i;
  
}